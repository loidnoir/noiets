/// RenderKit hosts the heavy rendering machinery (code syntax highlighting,
/// math, images, table fragments) added in M6. Placeholder until then.
public enum RenderKitModule {
    public static let name = "RenderKit"
}
